#include <stdio.h>
#include <ctype.h>  

int main() {
    char str[200];
    int i;

    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin); 

    
    for (i = 0; str[i] != '\0'; i++) {
        str[i] = tolower(str[i]);
    }

    
    if (str[0] >= 'a' && str[0] <= 'z') {
        str[0] = toupper(str[0]);
    }

    
    for (i = 0; str[i] != '\0'; i++) {
        if (str[i] == '.' || str[i] == '?' || str[i] == '!') {
            int j = i + 1;
            
            while (str[j] == ' ')
                j++;
            if (str[j] >= 'a' && str[j] <= 'z')
                str[j] = toupper(str[j]);
        }
    }

    printf("Sentence case: %s", str);
    return 0;
}
